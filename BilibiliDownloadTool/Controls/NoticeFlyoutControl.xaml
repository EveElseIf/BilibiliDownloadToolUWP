<UserControl
    x:Class="BilibiliDownloadTool.Controls.NoticeFlyoutControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BilibiliDownloadTool.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="300">

    <Grid MaxWidth="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock d:Text="TITLE" FontSize="25" Text="{x:Bind Title}" Margin="10,0,0,0"/>
        <TextBlock Grid.Row="1" d:Text="My Favourite band is Pastel*Palettes,all members are cute and working hard,that's why I love them much"
                   FontSize="15" TextWrapping="Wrap" Text="{x:Bind Text}" Margin="5"/>
    </Grid>
</UserControl>
