<Page
    x:Class="BilibiliDownloadTool.Pages.SearchHomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BilibiliDownloadTool.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchTextBox" PlaceholderText="输入url、bv、av、ss、ep进行搜索" 
                     FontSize="20"
                     VerticalAlignment="Center" Height="40" Width="auto"/>
            <Button x:Name="SearchBtn" Grid.Column="1" Content="🔍" Height="40" Width="40" Click="SearchBtn_Click"/>
            <ProgressRing x:Name="ProgressRing" Grid.Column="2" IsActive="False" Width="40" Height="40"/>
        </Grid>
    </Grid>
</Page>
